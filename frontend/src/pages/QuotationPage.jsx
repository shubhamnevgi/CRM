export default function QuotationPage() {
  return (
    <section>
      <h1>Quotation Builder</h1>
      <div className="card">
        <p>Template-based quotation UI scaffolded.</p>
        <p>Next step: product picker + quantity + total + PDF generation API integration.</p>
      </div>
    </section>
  );
}
